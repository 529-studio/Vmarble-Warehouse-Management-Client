# .env.staging.example
# Copy file này thành .env trên server: /opt/stacks/vwms-fe/.env
# KHÔNG commit file .env vào repo!

# ── App Configuration ──────────────────────────────────────────────────────────
BACKEND_URL=http://10.8.0.1:8080

# ── Deploy Configuration (cho deploy.sh) ──────────────────────────────────────
APP_IMAGE=ghcr.io/giangdq202/vmarble-warehouse-management-client:sha-xxxxxxx
SERVICE_NAME=frontend
HEALTH_URL=http://localhost:3000

