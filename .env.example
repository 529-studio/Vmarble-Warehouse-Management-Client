# Go backend API base URL
NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1

# JWT auth (set by backend, read client-side)
# NEXT_PUBLIC_AUTH_ENABLED=true

# Overflow alert threshold (mirrors backend REMNANT_OVERFLOW_THRESHOLD_PCT)
# NEXT_PUBLIC_OVERFLOW_THRESHOLD=15
